import { ControladorEstados } from './controlador-estados';

describe('ControladorEstados', () => {
  it('should create an instance', () => {
    expect(new ControladorEstados()).toBeTruthy();
  });
});
