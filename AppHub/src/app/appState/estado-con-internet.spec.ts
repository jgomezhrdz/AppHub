import { EstadoConInternet } from './estado-con-internet';

describe('EstadoConInternet', () => {
  it('should create an instance', () => {
    expect(new EstadoConInternet()).toBeTruthy();
  });
});
