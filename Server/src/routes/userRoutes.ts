import express, { Router } from 'express';


class userRoutes {

    router: Router = Router();

    constructor() {
        this.config();
    }

    config() {
        
    }

}

export default new userRoutes().router;
